function likes(names) {
    len = names.length
    switch (true) {
        case 0 === len:
            return 'no one likes this';
            case 1 === len:
                return '${names[0]}'
            
            break;
    
        default:
            break;
    }
}

var1 = [1,2,3,4]

console.log(`${var1[2]}`)